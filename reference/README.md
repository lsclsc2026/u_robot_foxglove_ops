# 历史参考材料

本目录只用于版本比对、来源追溯和理解旧部署过程，不是当前安装入口。原机器人地址、绝对路径、版本与“已验证”等措辞仅描述文档写作时环境。当前入口见[仓库 README](../README.md)和[范围说明](../docs/scope-and-limitations.md)。

`mos_ops` 是历史整理版，`lumos87_monitor_migration_20260812` 是 8 月 12 日迁移快照，`mos_ops_guard` 与 `_mos_vision_guard` 保存旧保护实现。`mos_fleet_monitor` 保存部署记录；9 个 tar.gz 历史重复副本已从发布中排除，原件留在输入归档。不要直接将这些脚本覆盖到当前部署。
